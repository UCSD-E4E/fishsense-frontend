version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:5000" 
    volumes:
      - .:/code 

  redis:
    image: "redis:alpine"